node_modules/
dist/
.measure/
.probe/

# Vendored source data. Everything here is regenerated by `npm run fetch`, and the two big
# archives are far too large to belong in git. The exact bytes and record counts are recorded
# in data/raw/*-fetch-manifest.json and in public/data/manifest.json, both of which ARE
# committed, so provenance survives without the payload.
data/raw/Slab2Distribute_Mar2018.tar.gz
data/raw/slab2-xyz/
data/raw/slab2-nodes/
data/raw/comcat/
data/raw/comcat-m45-1990-2026.csv
data/raw/*.log

.vercel
